// =======================
// Loop: for
// =======================

const doses: number[] = [250, 250, 500]; // mg ต่อครั้ง
let totalDailyDose: number = 0;

for (let i = 0; i < doses.length; i++) {
  totalDailyDose += doses[i];
}

console.log("Total daily dose (mg):", totalDailyDose);
